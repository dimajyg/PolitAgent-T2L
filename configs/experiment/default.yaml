name: "default"
description: "Default benchmark experiment"

runs_per_game: 1
workers: 1
max_phrases: null
debug: false

games:
  - askguess

tracking:
  enabled: true
  log_params: true
  log_metrics: true
  log_artifacts: true

tags:
  - "baseline"
  - "single_run" 